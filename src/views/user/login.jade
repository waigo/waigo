extends ../_layout

include ../_mixins

block title
  title Login

block main_content
  #login
    h1.ui.header Login

    if reason
      +msg('error', reason, 'Please login to gain access.')

    +form(form, error)

    a.signup(href="/user/register") Not yet registered? Sign up.