module.exports = {
  /** The folder containing the Waigo application source files. This is specified relative to the root app folder from which the node executable is run. */
  src: "src",
  /** Whether to log the actions of the Waigo module lazy-loader. Useful for diagnosing problems when overriding Waigo's built-in modules. */
  logLoader: false
};